int _isupper(int c);
/**
 * _isupper - checks for uppercase character.
 * @c: ascii nuber to be tested.
 *
 * Return: 1 i @c is uppercase or otherwise zero.
 */
int _isupper(int c)
{
	if (c is >= 65 && c <= 90)
	{
		return (1);
	}
	return (0);
}
