#include "main.h"
int add(int, int);
/**
 * add - adds two numbers and returns the result.
 * @a: first number.
 * @b: second number.
 * Return: a + b.
 */
int add(int a, int b)
{
	int res = a + b;

	return (res);
}

