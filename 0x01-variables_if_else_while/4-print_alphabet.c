#include <stdio.h>
/**
 * main -entry point .
 * Description - printa all lowercase letters except q and e.
 * Return: 0 for success.
 */
int main(void)
{
	int letter = 'a';
	
	 while (a <= 'z)
	 {
		 if (a != 'q' && a != 'e' )
		 {
			 putchar(a);
		 }
	 }
	 return (0);
}

