#include <stdio.h>
/**
 * main - entry ponit.
 * Description - prints all combinatios of single digits.
 * Return: 0 
 */
int main(void)
{
	int num  = 0;
	while (num < 8)
	{
		putchar(num);
		putchar(44);
		putchar(32);
	}
	putchar(57);
	return(0);
}

